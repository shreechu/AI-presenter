# Core (always required)
# (no external deps for simulation-only mode)

# PowerPoint slide loading
python-pptx>=0.6.21

# Local TTS (offline, cross-platform)
pyttsx3>=2.90

# Azure Neural TTS (optional)
# azure-cognitiveservices-speech>=1.37.0

# OpenAI TTS & LLM answers (optional)
# openai>=1.30.0

# Real microphone capture + Whisper transcription (optional)
# sounddevice>=0.4.6
# numpy>=1.24
# soundfile>=0.12
# openai-whisper>=20231117
